# Superwhisper Research Log

Started: 2026-05-25 19:00:35 EDT

Scope: local inspection of the installed Superwhisper app bundle and nearby readable metadata/configuration surfaces. Avoid dumping private transcript/audio content unless explicitly requested.

## Log

- 19:00: Located installed app bundle at `/Applications/superwhisper.app`.
- 19:01: Bundle metadata: `CFBundleIdentifier=com.superduper.superwhisper`, display name `superwhisper`, version `2.13.2`, executable `Contents/MacOS/superwhisper`, minimum macOS `13.3`.
- 19:01: Bundle registers URL schemes `superwhisper` and `superwhisper-debug`, owns public audio/video document types, and uses Sparkle updates from `https://superwhisper.com/appcast.xml`.
- 19:01: Code signing: Developer ID Application `Neil Chudleigh (XDP69BYUP9)`, notarization ticket stapled, timestamp `Apr 29, 2026 at 5:45:38 PM`, universal Mach-O for `x86_64` and `arm64`.
- 19:03: Entitlements are `com.apple.security.device.audio-input` and `com.apple.security.automation.apple-events`. The app is notarized and accepted by Gatekeeper.
- 19:04: Main app links `ArgmaxSDK`, `Sparkle`, `Sentry`, `libllama`, `ggml`, `onnxruntime 1.19.0`, `YbridOgg`, `YbridOpus`, `MediaRemoteAdapter`, WebKit, CoreML, ScreenCaptureKit, AVFoundation, Contacts, PDFKit, ServiceManagement, and private `MediaRemote`.
- 19:05: Readable resources include chat templates for `deepseek-r1`, `gpt-oss`, `llama-3.2`, `llama3`, `ministral`, `mistral`, and `phi-2`; bundled start/stop/notification sounds; `Archive.zip` with older/classic sound assets; `mediaremote-adapter.pl`; `claude-hook`; and `bundled_app_info.json`.
- 19:07: `mediaremote-adapter.pl` is an open Perl wrapper around `MediaRemoteAdapter.framework` for get/stream/send/seek/shuffle/repeat/speed/test operations against media playback state.
- 19:08: `claude-hook` is not a script; it is a compiled arm64 Swift helper. Strings show `ClaudeHook/main.swift`, `/tmp/superwhisper-agent`, `.claude/projects`, `.claude/plans`, `sessions-index.json`, `transcript_path`, `hook_event_name`, and URL scheme handoff via `superwhisper` / `superwhisper-debug`.
- 19:10: `bundled_app_info.json` is a large app classification/input-format catalog. Example entries classify Claude/ChatGPT as `AI Chat` with `chat_message`, Cursor/VS Code as `Text Editor (Code)` with `code`, and Terminal as `Terminal` with `terminal_command`.
- 19:13: Main executable is stripped of full source but embeds many Swift source paths under `/Users/ultra/Development/superwhisper/...`. Major modules include Accessibility, Audio, Database, Keyboard, Managers, Transcription, WebSocket, Views, and utilities.
- 19:14: Notable embedded module/class names include `AxManager`, `BuildSWUITree`, `CaptureEngine`, `Recorder`, `ScreenRecorder`, `DatabaseManager`, `HistoryManager`, `ModelManager`, `ModeManager`, `APIManager`, `LicenseManager`, `WhisperKitManager`, `WhisperContext`, `StreamingDiarizer`, `SileroVAD`, `DeepgramWebSocketManager`, `ElevenLabsWebSocketManager`, `AgentSessionManager`, and `AgentInlineDictation`.
- 19:15: Binary imports from `ArgmaxSDK` indicate `WhisperKitPro`, streaming transcription sessions, diarization, speaker segmentation, custom vocabulary, model repos, and CoreML-backed model loading.
- 19:16: User-level state found at `~/Library/Application Support/superwhisper`, `~/Library/Application Support/com.superduper.superwhisper`, `~/Library/Caches/com.superduper.superwhisper`, and `~/Library/Preferences/com.superduper.superwhisper.plist`.
- 19:17: `~/Library/Application Support/superwhisper` is about `968M`, mostly downloaded local model files under `models/argmaxinc` plus `emb-v1.onnx`, `seg-v1.onnx`, and `vad-v1.onnx`.
- 19:18: Local database is `~/Library/Application Support/superwhisper/database/superwhisper.sqlite`, about `188K`. Tables include `recording`, `recording_fts`, GRDB migration tables, and FTS backing tables. Row counts: 87 recordings and 87 FTS rows. Did not dump transcript/result contents.
- 19:19: Database schema stores recording metadata (`duration`, app/model/language model names, device, prompt, prompt context, folder name, processing times) and FTS text fields (`llmResult`, `rawResult`, `result`).
- 19:20: Current defaults show onboarding completed, active mode `default`, mini recorder enabled, recording view enabled, licenseValid `0`, and cached remote cloud model metadata. Did not preserve the full cache in this log.
- 19:21: No active `superwhisper` or `claude-hook` process was running during inspection.
- 19:05: Started focused payment/license sync investigation. Scope: inspect local license state and binary/API surfaces without printing license IDs, account IDs, tokens, or other payment secrets.
- 19:06: Payment/license state files under `~/Library/Application Support/com.superduper.superwhisper`: `argmax.anonymousId` JSON, `argmax.isLicensed` JSON, `argmax.licenseStatusCache` JSON, and empty `argmax.queueFolder`.
- 19:07: Redacted `argmax.licenseStatusCache` shape: one timestamp-like key plus `last_update` with `licenseEvent=token_creation`, `result=success`, `status_code=201`, and `has_pro_access=true`. `argmax.isLicensed` is currently `false`.
- 19:08: App defaults also currently report `licenseValid=false`. This suggests Superwhisper's own app license state and Argmax SDK token/pro-access cache are separate local states.
- 19:09: Keychain metadata confirms one generic-password item with service `com.superwhisper.vault`, created/modified `2026-05-23 01:27:40Z`. Did not request or print secret contents.
- 19:11: Binary strings identify `LicenseManager.swift` and endpoints on `https://api.superwhisper.com`: `/license/email/verify`, `/license/activate`, and `/license/validate`.
- 19:12: License activation UX uses `https://superwhisper.com/checkout`, `https://superwhisper.com/enterprise/login?from=app`, `superwhisper://license/`, and a Stripe customer portal URL. Strings say the app receives a deeplink license key, calls `activateLicense` against `https://api.superwhisper.com`, then posts a `licenseActivated` notification.
- 19:13: License validation logs include `Error on license validation check request`, `Error on license validation check, do nothing - API is down`, `License is invalid, set license invalid and return`, and `License validated with org:`. This implies API downtime preserves current state, while an explicit invalid server response clears validity.
- 19:14: Local vault key names embedded in the binary include `licenseKeyVaultKey`, `licenseValidVaultKey`, `usedFreeSecondsVaultKey`, `organizationVaultKey`, `inferenceAPIKey`, `inferenceAPIKeySetAt`, `inferenceV2APIKey`, and `inferenceV2APIKeySetAt`; legacy migration keys include `_legacy_licenseKey` and `_legacy_licenseValid`.
- 19:15: Organization validation payload fields visible in strings include `organizationId`, `organization`, `cloudModelsEnabled`, `syncStatsEnabled`, `recordingRetentionDuration`, and `enableCloudModels`.
- 19:16: Superwhisper app-level sync is gated by license validity. `FilesyncManager.swift` logs include `No valid license; skipping sync cycle`, `Failed to enable filesync, could not create user`, `Enabling filesync`, and `Filesync enabled`.
- 19:17: Filesync API strings include `/users/c...` (likely user creation), `/file/check-updates?since=`, and short Swift-string prefixes `/file/up...`, `/file/de...`, `/file/do...`; request header strings include `Bearer`, `X-ID`, `X-License`, and `X-Signature`. Exact short endpoints were not fully recoverable from `strings`.
- 19:18: Stats sync is organization-gated. Strings show `Stats sync disabled in org vault data`, `Stats sync starting for org:`, `Stats sync: querying DB for recordings since`, and endpoint `/stats/recording/bulk`.
- 19:19: Argmax SDK-level license/token sync is separate and uses `https://api.argmaxinc.com`, `/v1/license-token/create`, `/v1/license-token/refresh`, cached device info under `com.argmaxinc.sdk.DeviceInfo.cache`, and `ArgmaxSDK/LicenseJwtVerifier.swift`.
- 19:20: Practical inferred flow: payment/checkout happens on the website/Stripe; app receives or enters a license key; app activates and validates with Superwhisper API; key/valid/org data are persisted in Keychain vault; valid license enables file sync, org policy, cloud/pro models, and stats sync; Argmax SDK separately creates/refreshes a signed license token for local pro transcription SDK features.
- 19:11: Deep pass on payment/computer sync. Confirmed Superwhisper backend base `https://api.superwhisper.com` with license routes `/license/email/verify`, `/license/activate`, `/license/validate`, plus cloud entitlement routes `v1/inference/key`, `v2/inference/key`, and `models/language/cloud`.
- 19:11: Payment UX surfaces: `https://superwhisper.com/checkout`, `https://billing.stripe.com/p/login/aEU5mY54o5t19l6cMM`, enterprise login, and deeplink `superwhisper://license/` that triggers `activateLicense` then posts `licenseActivated`.
- 19:11: Local license state lives in Keychain service `com.superwhisper.vault` (one item present on this Mac, modified `2026-05-23`). Vault keys include `licenseKeyVaultKey`, `licenseValidVaultKey`, `organizationVaultKey`, `usedFreeSecondsVaultKey`, `inferenceAPIKey`, `inferenceV2APIKey`, and legacy migration keys.
- 19:11: Validation behavior from log strings: `attemptLicenseValidation` on a timer (`refreshTimer`/`BackgroundTimer`); explicit invalid response => `License is invalid, set license invalid and return`; API outage => `Error on license validation check, do nothing - API is down` (fail-open on network/server failure).
- 19:11: Org/policy payload fields inferred from strings: `organizationId`, `organization`, `cloudModelsEnabled`, `syncStatsEnabled`, `recordingRetentionDuration`, `enableCloudModels`, `enableSyncStats`, `licenseType`, `totalFreeSeconds`, `usedFreeSeconds`, `waitingForSubscription`.
- 19:11: Computer/account bridge is `FilesyncManager` + `FilesyncService`. Sync is disabled unless license is valid (`No valid license; skipping sync cycle`). Enabling sync first creates a server user (`Failed to enable filesync, could not create user`), then runs periodic cycles (`syncInterval`, `healthCheckInterval`, `healthCheckTimer`).
- 19:11: Filesync API surface (some Swift short-string prefixes truncated in binary): `/users/create` (inferred from `/users/c`), `/file/check-updates?since=`, `/file/upload`, `/file/delete`, `/file/download` (inferred from `/file/up`, `/file/de`, `/file/do`). Syncs settings/modes/models across installations on the same license; UI copy says "application files will be synced with any other installation registered to the same license."
- 19:11: Filesync auth appears custom, not plain Bearer-only: strings include `Bearer`, `X-ID`, `X-License`, `X-Signature`, plus `Utilities/Crypto.swift` and `sha256Pattern`. Device identity uses platform UUID with fallback (`Could not get Device UUID, falling back to generated`) and `installationID`.
- 19:11: Stats sync is separate from filesync and org-gated. Endpoint `/stats/recording/bulk`; defaults key `lastRecordingStatSync`; logs include chunking, "no new recordings since", and org-disable guard `Stats sync disabled in org vault data`.
- 19:11: Argmax SDK licensing is a second stack at `https://api.argmaxinc.com` with `/v1/license-token/create` and `/v1/license-token/refresh`, local cache files `argmax.anonymousId`, `argmax.isLicensed`, `argmax.licenseStatusCache`, JWT verification in `LicenseJwtVerifier.swift`, and token claims like `license_id`, `has_pro_access`, `access_token`, `refresh_token`.
- 19:11: Current local inconsistency on this machine: app defaults `licenseValid=0`, `argmax.isLicensed=false`, but redacted `argmax.licenseStatusCache.last_update` still records prior successful `token_creation` with `has_pro_access=true` and `status_code=201`. Treat Superwhisper app license and Argmax SDK token cache as independent states.
- 19:14: Created actionable gating research checklist at `research-superwhisper-gating-checklist.md` with baseline snapshot, static feature-to-gate matrix, runtime experiment protocol, and decision tree.
- 19:14: Baseline reconfirmed: `licenseValid=0`, `argmax.isLicensed=false`, keychain vault present, local Argmax pro model directories present under `~/Library/Application Support/superwhisper/models/argmaxinc/*`.
- 19:14: Static Pro gate strings mapped for free-tier limits (2 modes, no file transcription), paywall surfaces, cloud model `licenseType=pro`, SDK errors (`WhisperKitPro/SpeakerKitPro is not available with your SDK license`), filesync skip on invalid license, and org-controlled retention/stats.
- 19:15: Runtime pass: launched app successfully; `licenseValid` stayed `0`; no entitlement strings appeared in Unified Logging during short capture; no TCP connections observed at snapshot time. Likely needs HTTPS proxy or LLDB for deeper runtime evidence.
- 19:16: Experiment B continued via CFURL cache at `~/Library/Caches/com.superduper.superwhisper/Cache.db`. Cached requests at `2026-05-25 22:58:57` to `https://api.superwhisper.com/license/validate` (body `License not found`) and `https://api.superwhisper.com/models/language/cloud` (JSON catalog, ~3397 bytes).
- 19:16: Unsigned API probes: Superwhisper endpoints return `Invalid signature` (401) or `Error on license validation check` (500); Argmax token create returns `Authentication Failed` (400). Confirms signed-request auth boundary.
- 19:17: `nettop` shows modest background traffic while idle; persistent TCP not visible via `lsof`, likely short-lived URLSession connections. AppleScript UI inspection blocked without assistive access.
